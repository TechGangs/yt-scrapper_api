{
  "name": "yt-scrapper-api",
  "version": "1.0.0",
  "description": "YouTube audio downloader API via scraping (no API key required)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "youtube-dl-exec": "^3.0.24",
    "tmp": "^0.2.5"
  }
}